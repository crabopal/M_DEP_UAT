&НаКлиенте
Процедура СтатьиБюджета(Команда)
	Если ЗначениеЗаполнено(Объект.Ссылка) Тогда
		ПараметрыОткрытия = Новый Структура;
		ОткрытьФорму("Справочник.СтатьиБюджета.ФормаСписка",ПараметрыОткрытия,Объект.Ссылка,ЭтаФорма);
	Иначе
		Сообщение = Новый СообщениеПользователю;
		Сообщение.Текст = "Бюджет еще не записан";
		Сообщение.Сообщить();
	КонецЕсли;
КонецПроцедуры
